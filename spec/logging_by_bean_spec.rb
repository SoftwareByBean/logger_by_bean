require 'spec_helper'

describe LoggingByBean do
  it 'has a version number' do
    expect(LoggingByBean::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
